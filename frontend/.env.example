# Leave unset for local development — Vite proxies /api to http://localhost:5000 (see vite.config.js).
# In production, if the frontend is deployed on a different domain than the API,
# set this to the deployed API's base URL, e.g.:
# VITE_API_URL=https://api.example.com/api
VITE_API_URL=
